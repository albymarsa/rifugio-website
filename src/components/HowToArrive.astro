<section class="section section--alt">
  <div class="container">
    <span class="section-label">Come Arrivare</span>
    <h2 class="section-title">Raggiungi il rifugio</h2>
    <div class="arrive__grid">
      <div class="arrive__step">
        <span class="arrive__number">01</span>
        <h3>In auto</h3>
        <p>
          Raggiungi San Domenico in auto. Da qui parte il sentiero
          che in circa 2 ore di cammino porta al rifugio. Coordinate GPS:
          [DA AGGIUNGERE].
        </p>
      </div>
      <div class="arrive__step">
        <span class="arrive__number">02</span>
        <h3>Il sentiero</h3>
        <p>
          Segui il sentiero segnalato [DA AGGIUNGERE: numero e colore segnavia].
          Il percorso presenta un dislivello di [DA AGGIUNGERE] metri
          ed è adatto a escursionisti con esperienza base.
        </p>
      </div>
      <div class="arrive__step">
        <span class="arrive__number">03</span>
        <h3>Cosa portare</h3>
        <p>
          Scarponi da trekking, abbigliamento a strati, acqua sufficiente,
          sacco a pelo (il rifugio fornisce coperte ma non lenzuola),
          e una torcia frontale.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .arrive__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 3rem;
    margin-top: 2rem;
  }

  .arrive__step {
    position: relative;
  }

  .arrive__number {
    font-size: 3rem;
    font-weight: 800;
    color: var(--color-border);
    line-height: 1;
    display: block;
    margin-bottom: 1rem;
  }

  .arrive__step h3 {
    margin-bottom: 0.8rem;
    font-size: 1.1rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .arrive__step p {
    font-size: 0.95rem;
    color: var(--color-gray);
    line-height: 1.7;
  }

  @media (max-width: 768px) {
    .arrive__grid {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }
  }
</style>
